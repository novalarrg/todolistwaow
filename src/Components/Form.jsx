import { Box, Typography } from "@mui/material"
import React from "react"

const Form = () => {
  return (
    <Box sx={{ minWidht: "300px" }}>
      <Typography>titlle</Typography>
      <Typography>date</Typography>
      <Typography>description</Typography>
      <Typography>button</Typography>
    </Box>
  )
}

export default Form
